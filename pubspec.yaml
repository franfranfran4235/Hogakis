name: flutter_application_1
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3  # Agrega esta línea

flutter:

  uses-material-design: true

flutter_icons:
  android: true  # Habilita el icono para Android
  ios: true      # Habilita el icono para iOS (si es necesario)
  image_path: "assets/icon/app_icon.png"  # Ruta de tu imagen de icono
